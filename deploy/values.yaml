replicaCount: 3

image:
  repository: nginx
  tag: latest

service:
  type: ClusterIP
  port: 80

ingress:
  host: app.exemplo.com
  annotations:
    kubernetes.io/ingress.class: nginx

rollout:
  steps:
    - setWeight: 20
    - pause: {} # Pausa manual
    - setWeight: 50
    - pause: {} # Pausa manual
    - setWeight: 100